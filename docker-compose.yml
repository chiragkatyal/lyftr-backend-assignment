version: "3.8"
services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - data:/data
    environment:
      DATABASE_URL: sqlite:////data/app.db
      WEBHOOK_SECRET: testsecret
volumes:
  data:
